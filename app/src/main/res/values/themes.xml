<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.ValorantX" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/redC</item>
        <item name="colorPrimaryVariant">@color/redC</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.Valorantx." parent="Theme.ValorantX">
        <item name="android:color">@color/white</item>
    </style>

    <style name="App.Custom.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/dark_blue</item>
    </style>


    <!--
    <style name="SplashTheme" parent="Theme.Valorantx">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>
    -->

</resources>